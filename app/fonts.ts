import { Fira_Code, Mona_Sans } from "next/font/google";

// global font
export const firaCode = Fira_Code({ subsets: ["latin"] });
// element font
export const mono = Mona_Sans({ subsets: ["latin"], weight: ["400", "700"] });
